// This file is kept for backward compatibility
// The actual header is in common/Header.jsx
import Header from './common/Header';

const Navbar = () => {
  return <Header />;
};

export default Navbar;
